Available Files: taskpane.css, taskpane.html, taskpane.js

taskpane.html file contains the HTML markup for the Outlook add-in 
taskpane.css file contains the CSS that's applied to content in the task pane 
taskpane.js file contains the Office JavaScript API code that facilitates interaction between the task pane and Outlook.

To compile the project install required software: Enter "npm install -g yo generator-office" in command prompt Enter "yo office" in command prompt

The project has one no required arguments. To run the project, ENTER "npr start" in the root folder of the project

This project will then launch an instance of outlook.

To test impcryptr Click the "Show Task Pane" Icon.
